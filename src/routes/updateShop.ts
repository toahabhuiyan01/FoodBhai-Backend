import { Handler } from "../api/make-api";
import { Shop } from "../types/openapi-client";

const handler: Handler<'updateShop'> = async(
    
) => {
    
    return {
        id: '',
        name: '',
        
    } as Shop
}

export default handler